# upstream-pulse — sync-2026-04-11-wren

## Sources Checked

| Source | From | To | Delta |
|--------|------|----|-------|
| psmux/psmux | c6088d5 | 5f8c20d | 3 new commits |
| Claude Code | 2.1.100 | 2.1.101 | 1 release (no tmux/agent changes) |
| Pi Agent | 0.66.1 | 0.66.1 | no changes |

## Upstream HEAD

`5f8c20d` fix: force hook run-shell commands to background mode

## New Upstream Commits

1. `f81632a` fix(set-hook): preserve quoting in hook commands for paths with spaces
2. `3bcff11` fix: preserve quoting in server-side set-hook handlers
3. `5f8c20d` fix: force hook run-shell commands to background mode

## Tiered Analysis

### TIER 1: MERGES (0 items)

(none)

### TIER 2: QUICK CONFIG (0 items)

(none)

### TIER 3: LOW COMPLEXITY / HIGH IMPACT (1 item)

- **psmux/psmux `5f8c20d`**: Force hook run-shell commands to background mode
  - **Reasoning**: Adds `ensure_background()` helper (self-contained, 18 lines) that injects `-b` into run-shell commands fired by hooks. Prevents "running: ..." status bar noise and unwanted output popups. Refactors `server/mod.rs` to use `fire_hooks()` instead of inline hook iteration. Clean UX improvement for any plugin/hook workflow.
  - **Action**: Port `ensure_background()` function to ohboy-builds `commands.rs`. Refactor inline hook loops in `server/mod.rs` to use `fire_hooks()`. Both files diverged — manual port required.
  - **Regression risk**: CustomPaneBackend (server/mod.rs dispatch), Agent Orchestration (command dispatch)
  - **Files**: `src/commands.rs` (+27/-2), `src/server/mod.rs` (+19/-16)

### TIER 4: FIXES (2 items)

- **psmux/psmux `f81632a`**: Preserve quoting in set-hook commands for paths with spaces
  - **Reasoning**: `non_flag[1..].join(" ")` loses quotes around paths like `"C:\Program Files\Psmux Plugins\..."`. Fix extracts hook command from raw cmd string instead. Important on Windows where spaces in paths are common.
  - **Action**: Port quoting-aware extraction to ohboy-builds `commands.rs` set-hook handler. File diverged — manual port.
  - **Regression risk**: Agent Orchestration (hooks used for agent lifecycle)
  - **Files**: `src/commands.rs` (+12/-3)

- **psmux/psmux `3bcff11`**: Preserve quoting in server-side set-hook handlers
  - **Reasoning**: Same quoting fix for the TCP/server code path in `connection.rs`. Also adds re-quoting in `dispatch_control_command()` for the control mode path.
  - **Action**: Port to ohboy-builds `server/connection.rs`. File heavily diverged — manual port.
  - **Regression risk**: CustomPaneBackend (connection.rs handles backend commands), Agent Orchestration
  - **Files**: `src/server/connection.rs` (+14/-3)

### TIER 5: FEATURES (0 items)

(none)

## Claude Code v2.1.101 (2026-04-10)

No TeammateTool, tmux backend, or agent spawning changes. Notable additions:
- `/team-onboarding` command
- `/ultraplan` auto-environment creation
- Brief/focus mode improvements
- OS CA certificate store trust
- Rate-limit and refusal message improvements

No action required for ohboy-builds.

## Pi Agent v0.66.1

No changes since last check. Unreleased: Antigravity User-Agent bump (v1.21.9), Gemma 4 thinking levels fix. Neither affects psmux integration.

## Regression Risk

| Feature | Status | Detail |
|---------|--------|--------|
| CustomPaneBackend | ⚠ AT RISK | `server/mod.rs` and `server/connection.rs` touched by 5f8c20d, 3bcff11 |
| Agent Orchestration | ⚠ AT RISK | `commands.rs` and `server/connection.rs` touched by all 3 commits |
| Remote tmux Control Mode | ✓ Safe | No risk files touched |
| DCS Passthrough | ✓ Safe | No risk files touched |
| Three-State Focus Borders | ✓ Safe | No risk files touched (server/mod.rs changes are in hook code, not rendering) |
| Session Resurrection | ✓ Safe | No risk files touched |
| Hints Mode | ✓ Safe | No risk files touched |
| Mycel Event Bus | ✓ Safe | No risk files touched |
| Popup-as-Pane | ✓ Safe | No risk files touched |
| Enhanced Format Engine | ✓ Safe | No risk files touched |
| Pane Focus Visibility | ✓ Safe | No risk files touched |
| VT Terminal State Tracking | ✓ Safe | No risk files touched |
| Encoding-Aware Mouse | ✓ Safe | No risk files touched |

## Merge Strategy

All 3 commits touch files that have diverged significantly between upstream and ohboy-builds. None qualify for cherry-pick or auto-merge. All decomposed into backlog tasks for manual porting.

| Commit | Category | Action |
|--------|----------|--------|
| f81632a | Overlapping fix | Manual port → backlog |
| 3bcff11 | Overlapping fix | Manual port → backlog |
| 5f8c20d | Overlapping fix | Manual port → backlog |
